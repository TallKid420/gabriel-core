[project]
name = "gabriel-core"
version = "0.1.0"
dependencies = [
	"pydantic",
	"fastapi",
	"httpx",
	"pytest",
	"alembic",
	"sqlalchemy[asyncio]",
	"asyncpg",
	"langgraph",
]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]